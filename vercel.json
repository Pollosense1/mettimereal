{
  "version": 2,
  "builds": [
    {
      "src": "mettime/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "mettime/build" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}